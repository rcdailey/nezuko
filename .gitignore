# Don't care about vs code workspace files at the root
*.code-workspace
*.stackdump

# Ignore contents of all subdirectories by default, which is usually the compose projects.
*/*

# Do not ignore these files/dirs
!.github/**
!.vscode/**
!_kubernetes/**
!*/docker-compose.yml
